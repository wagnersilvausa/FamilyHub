import type { Config } from 'tailwindcss'

// NOTE: This project uses Tailwind v4.
// All custom tokens (colors, shadows, animations) are defined in globals.css via @theme.
// This file is kept minimal to avoid conflicts.
const config: Config = {
  plugins: [],
}

export default config
